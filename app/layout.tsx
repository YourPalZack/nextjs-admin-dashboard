import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Colorado Job Board - Find Blue-Collar Jobs in Colorado",
  description: "The premier destination for blue-collar workers in Colorado to find employment opportunities in construction, manufacturing, transportation, and skilled trades.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}